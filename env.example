# Jarvis Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# LLM Configuration (Ollama via Colab)
# ============================================

# URL to your Colab Ollama instance.
# The Colab notebook provides TWO URLs. Use the one labeled "Ollama URL".
# This is the recommended and simplest setup.
# Example: https://a5d748f2fc77.ngrok-free.app
OLLAMA_HOST=https://YOUR-OLLAMA-NGROK-URL

# Optional: Specify model name (defaults to phi if not set)
# OLLAMA_MODEL=phi

# ---
# Alternative LLM Configuration (Flask Proxy)
# ---
# If you prefer to use the Flask proxy endpoint, uncomment the following line.
# This uses a custom adapter and may be less stable.
# OLLAMA_HOST=https://YOUR-FLASK-NGROK-URL/proxy_ollama
# ---


# ============================================
# Vision Configuration (BLIP-2 via Colab)
# ============================================

# URL to your Colab Flask server (same as OLLAMA_HOST but without /proxy_ollama)
# Example: https://ecf1b50b3d6c.ngrok-free.app
VISION_URL=https://YOUR-FLASK-NGROK-URL


# ============================================
# Azure Speech Services (TTS)
# ============================================

# Azure Cognitive Services Speech API Key
# Get from: https://portal.azure.com → Cognitive Services → Speech
AZURE_SPEECH_KEY=your_azure_speech_key_here

# Azure region where your Speech service is deployed
# Common regions: eastus, westus, westeurope, southafricanorth
AZURE_REGION=southafricanorth

# Azure TTS voice to use
# List: https://learn.microsoft.com/en-us/azure/ai-services/speech-service/language-support?tabs=tts
# Examples: en-US-JennyNeural, en-US-GuyNeural, en-GB-RyanNeural
AZURE_VOICE=en-US-JennyNeural


# ============================================
# Optional: Speech-to-Text Configuration
# ============================================

# By default, Faster Whisper (local) is used for STT
# If faster-whisper is not available, falls back to Google Speech API
# No configuration needed - automatic fallback


# ============================================
# Notes
# ============================================

# 1. OLLAMA_HOST and VISION_URL come from the same Colab notebook
#    - Run jarvis_colab_simple_setup.ipynb
#    - After Cell 7, copy the Flask URL
#    - OLLAMA_HOST = Flask URL + "/proxy_ollama"
#    - VISION_URL = Flask URL (without /proxy_ollama)

# 2. Azure Speech is the only paid service (~$0.50-$1/month)
#    - Local PowerShell TTS is used as fallback if Azure fails

# 3. Keep this file secure and never commit it to git
#    - .env is already in .gitignore

